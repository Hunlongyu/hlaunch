# 质量、安全与验证

## 证据等级

必须区分：静态检查、编译、单元测试、集成测试、运行验证、性能测量和多设备/多显示器验证。完成较低等级不能宣称较高等级已通过。

2026-09-08 三架构发布流程：新增标签、CMake 版本和日志一致性校验，版本准备/拒绝非法标签/日志隔离/版本溢出/三架构资产集合 5 项 Python 测试通过，actionlint 1.7.12 工作流检查通过。本机 MSVC 19.51 的独立 x64、x86 Release 构建均通过且各 11/11 项非交互桌面 CTest 通过；文件分别为 1,063,936 和 916,992 字节，PE Machine、内嵌 Manifest、0.1.19 版本资源与无动态 CRT 导入检查通过。该版本号仅是本机验证基线，本次没有递增版本或发布标签。本机未安装 ARM64 工具链，ARM64 交叉编译与 GitHub Release 端到端结果待首次正式标签运行验证；ARM64 真机及四项桌面相关测试本轮未执行。

当前验证证据（2026-08-28）：MSVC x64 Debug 的 15 个 CTest 目标全部通过，覆盖依赖、数据编解码与存储、日志、搜索、条目添加/编辑/删除/重排、Shell/WIC 图标提取与后台回调、OLE 拖放、窗口与输入、快捷键、边缘唤起、当前用户开机启动、托盘和 Shell 启动。Launcher 窗口集成测试验证 `Insert` 添加、`F2` 编辑、`Delete` 取消与确认、相邻焦点、全部编辑字段、同 Tab 鼠标重排、跨 Tab 拖动、边缘用户设置、开机启动设置和稳定 ID；Core 测试覆盖最终索引语义、稳定 `gridSlot`、远端空槽精确放置、占用槽连续顺移、批量目标槽顺序、跨 Tab 精确位置及无效移动不修改文档。配置测试覆盖材质、透明度、Grid 行列、默认值、往返、非法范围和旧条目缺失 `gridSlot` 的兼容读取。主题、背景图、自定义字体和组件样式模块及其测试已经移除。

置顶启动行为补充证据（2026-08-29）：Launcher/UI 窗口测试验证未置顶时成功启动后隐藏、置顶时成功启动后保持显示，以及 `Esc`、`WM_CLOSE` 仍可显式隐藏；隐藏后再次显示仍保留 `WS_EX_TOPMOST`。MSVC x64 Debug 编译及 15/15 CTest 通过。真实 Shell 目标启动后的连续置顶观感仍可由人工操作补充确认。

稳定性与发布边界补充证据（2026-08-29，v0.1.6）：UI Automation 共享 Provider 状态增加同步与断开保护，WndProc、COM 方法、`wWinMain` 以及图标、拖放解析、配置和条目保存线程均建立异常边界；后台测试验证抛出异常的保存通知和图标完成回调不会终止工作线程。Windows 注销/关机路径会刷新最终保存队列，高版本 schema 启动失败会明确显示文件路径并保证不覆盖。MSVC x64 Debug 与 Release 均编译通过且各 15/15 CTest 通过；针对本次入口的 clang-tidy exception-escape/use-after-move 检查无问题。Release 配置阶段拒绝非 x64，实际编译命令使用静态 CRT `-MT`；便携目录仅含一个 1,078,272 字节（1.028 MiB）的 `HLaunch.exe`，PE 为 x64 PE32+，版本资源为 0.1.6，`dumpbin /dependents` 仅列出 Windows 系统 DLL，SHA-256 为 `CF203CF624E7D2EA7ABAA46AE9FB8033DD80BE4887A2D57FD00885C7D08099E3`。本轮没有新增第三方依赖、联网能力、更新器、插件系统或额外运行时文件；未重新执行空闲内存、热显示和大数据搜索基准，不能据此宣称这些性能指标已经改善。

标题栏置顶图标比例补充证据（2026-08-29，v0.1.7）：置顶和取消置顶字形由与菜单/关闭共用的 14 DIP 格式分离为 12 DIP 专用格式，以抵消 `E718`/`E77A` 更大的可见轮廓；标题栏高度、垂直校正、16 × 16 DIP 按钮命中区域、4 DIP 按钮间距和 UI Automation 边界均不变。自动测试固定置顶字形尺寸小于通用标题图标尺寸，MSVC x64 Debug 与 Release 均编译通过且各 15/15 CTest 通过。Windows 应用控制工具未能识别该自绘分层窗口，最终真实窗口比例仍需人工确认。

快捷键下拉列表补充证据（2026-08-29，v0.1.8）：候选项由容易把数字压到字母之后的顺序调整为 `Space → 0–9 → A–Z → F键`，展开时强制回到顶部；下拉高度保证前 11 项完整可见，列表超过可见高度时继续使用原生垂直滚动。Launcher/UI 测试验证 60 个候选项、Space/0/9 的固定位置、前 11 项所需高度、滚动到后续项目以及重新展开后的顶部复位；MSVC x64 Debug 与 Release 均编译通过且各 15/15 CTest 通过。

屏幕边缘设置精简补充证据（2026-08-29，v0.1.9）：设置窗口移除边缘宽度、角落大小、停留、采样和冷却五个调节项，内部固定为 4 DIP、16 DIP、300 ms、40 ms、500 ms。schema v1 兼容字段继续写入固定值以支持旧版本回退；新版读取时忽略历史自定义值。Launcher/UI 测试覆盖控件移除和应用时固定值归一化，配置测试覆盖旧值忽略与保存写回；MSVC x64 Debug 与 Release 均编译通过且各 15/15 CTest 通过。

快捷键列表滚轮补充证据（2026-08-29，v0.1.10）：按键下拉框增加原生 `WS_VSCROLL` 与 `CBS_NOINTEGRALHEIGHT`，固定弹出高度并显示可用的垂直滚动列表；展开通知同时立即及延后复位顶部，避免原生控件随后按当前选中项重新定位。真实窗口测试取得 `ComboLBox` 句柄，验证垂直滚动样式，并确认收到向下 `WM_MOUSEWHEEL` 后顶部索引从 0 增大。MSVC x64 Debug 与 Release 均编译通过且各 15/15 CTest 通过。

进程名单多行编辑补充证据（2026-08-29，v0.1.11）：边缘唤起的黑名单与白名单由单行输入框改为 58 DIP 高的原生多行 `EDIT`，支持回车换行、自动垂直滚动和可见滚动条；解析继续兼容换行、逗号和分号，回显统一为 CRLF 每行一项。窗口测试覆盖样式、高度、混合分隔解析、去重和重新打开后的两行格式；MSVC x64 Debug 与 Release 均编译通过，Debug 首轮仅既有原子存储用例偶发失败，单独复现及随后完整 15/15 复跑通过，Release 15/15 通过。

设置窗口宽度收紧补充证据（2026-08-29，v0.1.12）：选项窗口客户区由 620 DIP 收紧到 540 DIP，高度保持 610 DIP；Tab、各 GroupBox、长说明、透明度滑块、名单多行输入框及底部按钮同步内收 80 DIP。真实窗口测试固定 540 DIP 客户区宽度，并继续枚举两个页签的所有可见子控件，要求全部位于客户区内。MSVC x64 Debug 编译及 15/15 CTest 通过；Release 编译通过，前两轮分别出现既有原子存储首次保存波动和 Tooltip 桌面定位 13 px 波动，第三轮完整 15/15 通过，本次没有扩散修改这两个无关模块。

设置窗口底部留白收紧补充证据（2026-08-29，v0.1.13）：选项窗口客户区高度由 610 DIP 缩短到 590 DIP，底部状态和按钮坐标保持不变，使最下方控件到客户区底边的留白由约 34–36 DIP 降到约 14–16 DIP，同时不把空白转移到按钮上方。真实窗口测试固定 540 × 590 DIP 客户区并继续验证所有可见控件不越界；MSVC x64 Debug 与 Release 均编译通过且各 15/15 CTest 通过。

设置分组框裁切修复证据（2026-08-29，v0.1.14）：选项窗口宽度由 540 DIP 回调至 560 DIP，各横向容器与输入控件同步增加 20 DIP；屏幕边缘分组框右边界由 512 DIP 扩展到 532 DIP，使最右侧“右下”复选框与边框恢复 16 DIP 间距。窗口测试除固定 560 × 590 DIP 客户区和全控件客户区边界外，新增“右下”控件必须位于所属 GroupBox 右边界内至少 12 DIP 的约束；MSVC x64 Debug 与 Release 均编译通过且各 15/15 CTest 通过。

屏幕边缘灵敏度补充证据（2026-08-29，v0.1.15）：内部固定停留时间由 300 ms 缩短到 180 ms，采样间隔由 40 ms 缩短到 30 ms，正常触发窗口约为 180–210 ms；边缘宽度、角落尺寸和 500 ms 冷却保持不变，继续要求稳定停留并在触发后离开热区才能重新武装。状态机测试新增 179 ms 不触发、180 ms 触发的默认时序边界；配置测试覆盖历史值忽略及写回新固定值。首轮 Release 的无关拖放测试暴露仅用进程号命名临时目录导致的历史残留冲突，测试目录加入高精度计数器后，MSVC x64 Debug 与 Release 均重新完整编译且各 15/15 CTest 通过。

屏幕角落重复唤起补充证据（2026-08-29，v0.1.16）：针对快速返回左上角时偶发需要重新移动的问题，内部冷却由 500 ms 缩短到 100 ms；状态机把离开已触发热区后返回同一区域的情况记为已离开，并在冷却结束的同一次采样直接进入 Pending，避免额外等待一个采样周期。新增“冷却前返回、100 ms 时立即重新计时、180 ms 后再次触发”的回归序列。MSVC x64 Debug 完整 15/15 CTest 通过；Release 首轮仅无关的置顶窗口状态断言偶发失败，单独复跑及随后完整 15/15 CTest 均通过。

设置命名统一补充证据（2026-08-29，v0.1.17）：托盘菜单、Launcher 主菜单和对话框标题统一使用“设置”，主菜单保留 `Ctrl+O` 快捷键提示；窗口测试同时锁定菜单文字 `设置...` 和标题 `HLaunch 设置`，避免不同入口再次出现“设置 / 选项”混用。首轮 Debug 窗口测试受工作区外正在运行的同身份 HLaunch 实例干扰；关闭该确切进程后，MSVC x64 Debug 与 Release 均完整编译且各 15/15 CTest 通过。

设置窗口材质入口补充证据（2026-08-28）：原生“常规”页提供 Solid、Mica、Acrylic、Tabbed 四种系统背景效果，控件测试覆盖四个选项、当前 Acrylic 回显、页签显隐及 Mica 与透明度组合应用；运行时继续复用既有窗口效果和配置保存链路。各材质的最终桌面混合效果仍需 Windows 10/11 人工截图确认。

设置窗口 Tab 页面背景补充证据（2026-08-28）：窗口客户区与 Tab 内容区统一使用 `COLOR_BTNFACE` 系统表面；页面内仅 `STATIC`、GroupBox 和 Checkbox 使用 `WS_EX_TRANSPARENT` 与空画刷叠加，不再分别用 `COLOR_WINDOW` 或控件底色形成浅色块；Trackbar、禁用输入框等虽然也可能发送 `WM_CTLCOLORSTATIC`，仍使用不透明页面画刷，输入框、下拉框和底部命令按钮保留 Windows 原生表面。控件测试覆盖对话框与底部状态文字画刷、内容区画刷、说明文字与复选框的透明背景，以及 Trackbar 的不透明页面背景；不同 Windows 版本主题下的最终像素仍需人工确认。

设置窗口紧凑输入补充证据（2026-08-28）：单行 `EDIT` 从 26 DIP 收紧到 22 DIP并保持原中心线，下拉框请求 16 DIP 选择区项高并接受 Windows 按系统字体最低高度钳制；控件测试按当前窗口 DPI 检查实际输入框高度，并把 ComboBox 选择区限制在 16–20 DIP。系统字体与不同 DPI 下的最终基线观感仍需人工确认。

主窗口拖动区域补充证据（2026-08-28）：布局测试覆盖真实 Item 不可拖窗、空 Item 与 Grid 间隙可拖窗，真实窗口消息测试验证占用槽与空槽保持 `HTCLIENT`、Grid 间隙返回 `HTCAPTION`，空槽按下建立捕获并在松手时释放。空槽超过系统阈值后的物理鼠标移动仍需人工确认。

搜索输入表面补充证据（2026-08-28）：父窗口使用预乘 Alpha D2D 目标时，普通 GDI `EDIT` 只更新 RGB 会令请求的 `#303030` 经 DWM 合成为 `#848484`。输入子窗口现使用 `WS_EX_LAYERED` 与 255 Alpha，并继续由 `WM_CTLCOLOREDIT` 返回 `#303030` 实心画刷；正式主程序运行实例的屏幕取样已从 `#848484` 修正为 `#303030`。自动测试覆盖实心画刷和不透明背景模式；测试 EXE 没有主程序的 Windows 8+ 清单，不能用它验证分层子窗口。高对比度模式恢复 Windows 系统输入背景，最终光标闪烁和 IME 观感仍需人工确认。

Shell 与单实例补充证据（2026-08-28）：Shell 启动测试 10/10、64 个断言通过，包含真实 `.lnk` 创建、目标/参数/工作目录解析和 Shell 回退；Launcher/UI 测试 46/46、386 个断言通过，包含主激活窗口延迟 40 ms 创建时的有限重试、Toggle 投递和重试耗尽错误分类。不同完整性级别 UIPI 仍需人工验证。

图标测试还覆盖数据根磁盘缓存二次命中、损坏缓存重建、有界淘汰和旁路文件保护。全局快捷键测试通过真实 `RegisterHotKey` 的互斥行为验证注册、冲突恢复和重试；物理按键与 `WM_HOTKEY` 投递留在人工矩阵，避免 `SendInput` 受桌面会话或完整性级别影响产生伪失败。

混合 DPI 图标补充证据（2026-08-28）：Grid 图标固定为 32 DIP，并对 96/120/144/168/192 DPI 分别请求 32/40/48/56/64 px；显式 ICO/PNG 直接使用 WIC 解码和最佳帧选择，Shell 目标优先使用 `IShellItemImageFactory`，最终保持宽高比单次 Fant 缩放并对齐物理像素。Shell HBITMAP 按直通 Alpha 解释，所有来源先转预乘 BGRA 再缩放，缓存格式提升到 v2 以失效旧错误像素。UI 内存缓存按稳定条目 ID 与物理尺寸保留独立 LRU 变体，异步结果继续校验源与尺寸。Tab/Item 共享的顶层拖拽预览现在独立处理 `WM_DPICHANGED`、目标显示器 DPI、D2D DPI 和图标变体。MSVC x64 Debug 编译及 15/15 CTest 通过，覆盖透明白色测试像素的预乘/缩放、五档尺寸换算、ICO 五档解码、32/64 px 磁盘变体和拖拽预览 DPI 建议矩形；真实 Debug 重新生成的 28 个 32/40 px v2 缓存逐像素检查为预乘违规 0、透明 RGB 残留 0。真实多物理屏幕跨屏移动与最终像素观感仍需人工确认。

Grid 缩放补充证据（2026-08-27）：Core 测试覆盖增加列时保持二维坐标、减少列时仅迁移被裁列条目并保留更早空洞；UI 几何测试覆盖 96/144 DPI、右边与左上角量化；真实 Launcher 窗口消息测试覆盖边缘 `WM_NCHITTEST`、`WM_SIZING`、松手前目标位置预览、最终行列保存回调和 `gridSlot` 重排。物理鼠标八方向拖动及跨显示器 DPI 变化仍需人工验证。

Tab 与条目拖拽反馈补充证据（2026-08-27）：几何测试覆盖向左/向右及拖回源位置时的插入边界与最终索引映射、浮动 Tab 预览中心随鼠标横纵坐标连续移动，以及条目浮动卡片保持源格尺寸并跟随鼠标；真实窗口测试覆盖鼠标穿透顶层预览窗口越过 Launcher 客户区边界并在松手后隐藏。浮层实现复用源 Tab 的活动状态样式和源条目的普通表面/边框样式；像素级视觉一致性及跨显示器 DPI 下的真实鼠标效果仍需人工确认。

Tab 溢出交互补充证据（2026-08-28）：布局测试覆盖 64 DIP 最小宽度、首尾可见窗口、隐藏项无命中矩形和可见索引映射；真实 Launcher 窗口消息测试覆盖无 `WS_HSCROLL`、鼠标滚轮进入溢出分类、移动可见区后的首项点击映射，以及 `Tab` 键跨尾首循环。`Shift+Tab` 反向循环由纯函数测试覆盖；多显示器 DPI 下的文字裁切和真实滚轮手感仍需人工确认。

Tab 删除策略补充证据（2026-08-28）：策略测试覆盖多 Tab 文档中的空页面直接删除、非空页面要求确认、越界索引不可删除以及仅剩一个页面时不可删除；MSVC x64 Debug 全部 15 个 CTest 目标通过。

Grid/Tab 间距补充证据（2026-08-28）：默认 394 × 594 DIP、5 × 8 布局的几何测试验证最后一行条目底边与 Tab 顶部相距 8 DIP；量化窗口尺寸、定制 Grid/Tab 尺寸和 144 DPI 缩放测试同步通过。默认布局测试同时验证 16 DIP 标题高度和菜单按钮尺寸。

标题区收紧补充证据（2026-08-28）：主菜单已由手绘方块改为与置顶、关闭按钮共享的系统字体图标格式，使用兼容字形 `E700`，并改用与标题及右侧默认图标相同的弱化文字刷；布局测试覆盖 16 DIP 标题区、Grid 起点、默认/增量/定制窗口高度和 144 DPI 量化，MSVC x64 Debug 全部 15 个 CTest 目标通过。字体字形和颜色的实际观感仍需人工确认。

标题按钮反馈补充证据（2026-08-28）：渲染路径不再为主菜单、置顶或关闭按钮绘制悬浮背景；纯函数测试覆盖默认弱化色、菜单/置顶悬浮亮色、置顶状态强调色和关闭悬浮警示色，MSVC x64 Debug 全部 15 个 CTest 目标通过。真实鼠标按住及高对比度观感仍需人工确认。

Item 高亮补充证据（2026-08-28）：普通模式的真实 Item 悬浮和键盘焦点轮廓固定使用 `#FFFFFF`，拖拽落点保留强调色；普通、悬浮及拖拽外框均使用内描边几何，第一行顶部描边不会再有一半落到 Grid 裁切区外。视觉契约与几何测试覆盖白色语义、高对比度系统色、2 DIP 描边四边等距缩进和第一行顶部完整落在 Grid 内；MSVC x64 Debug 15/15 CTest 通过，最终像素观感仍需人工确认。

Tab 视觉补充证据（2026-08-28）：内置视觉测试固定普通背景 `#252525`、悬浮背景 `#414141`、选中线 `#0D7FD9`，布局指标固定 30 DIP 高度、0 DIP 间隙、0 DIP 圆角和 4 DIP 高亮线；主界面、搜索结果 Tab 和活动 Tab 拖拽预览共用矩形样式，高对比度模式使用系统色。视觉契约测试 44/44、380 个断言及 MSVC x64 Debug 全部 15 个 CTest 目标通过；像素级观感和真实鼠标悬浮仍需人工确认。

主界面固定配色补充证据（2026-08-28）：普通模式视觉契约固定主窗口背景基色为 `#373737`、Tab 表面基色为 `#252525`、Grid Item 表面基色为 `#515151`；Acrylic 等非 Solid 模式会在透明客户区上叠加背景基色并保留系统材质混合，高对比度继续使用系统色。视觉契约测试 44/44、374 个断言及 MSVC x64 Debug 全部 15 个 CTest 目标通过；不同桌面背板下的最终混合像素仍需人工确认。

标题按钮间距补充证据（2026-08-28）：默认布局测试验证置顶与关闭按钮之间固定 4 DIP 空隙，空隙命中仍属于无边框窗口拖动区域；MSVC x64 Debug 全部 15 个 CTest 目标通过。实际视觉比例仍需人工确认。

标题栏垂直对齐补充证据（2026-08-28）：标题文字、主菜单、置顶和关闭字体图标共用向上 1 DIP 的视觉校正，布局测试验证校正后的绘制范围与原命中范围高度一致且仅改变纵向位置；MSVC x64 Debug 全部 15 个 CTest 目标通过。Segoe 字形的最终视觉重心仍需在真实窗口人工确认。

Item Tooltip 补充证据（2026-08-28）：真实窗口测试验证 Item 名称继续由原生 `tooltips_class32` 承载且未启用 `TTS_BALLOON`，显示位置位于 Item 底部 4 DIP 并与 Item 水平中心对齐；悬浮初始延迟固定为 150 ms，外观不做应用级自绘或圆角控制。MSVC x64 Debug 全部 15 个 CTest 目标通过。

Grid 分页视觉补充证据（2026-08-28）：条目超过缩小窗口的当前容量时不再绘制右侧分页轨道和高亮条，也不启用原生 `WS_VSCROLL`；`PageUp`/`PageDown`、键盘焦点自动翻页和搜索结果跨页行为继续保留。MSVC x64 Debug 全部 15 个 CTest 目标通过。

拖放人工验证补充证据（2026-08-29）：用户在真实 Launcher 中验证 Explorer 文件、文件夹和多文件拖入，拖到指定 Tab、空槽和已占用槽位，文件拖到已有程序条目后作为一次性启动参数，以及 Unicode 路径和重复项目处理均通过；浏览器 URL 拖入此前亦已人工验证通过。结合既有 OLE 数据对象、解析器、Core 槽位变更和真实窗口注册自动测试，`PROD-DROP-001` 更新为已验证。

既有运行证据还覆盖双实例转发、快捷键 Toggle、边缘 Show-only、托盘 Toggle、搜索附属窗对齐、Grid 分页、四种 DWM 材质属性和仅系统 DLL 的 Release 依赖。物理快捷键、多显示器完整矩阵、Explorer 重启、Narrator/Accessibility Insights、高对比度、UAC 取消和失效目标矩阵仍未完成，不能宣称已验证。

快捷键定位补充证据（2026-08-27）：纯几何测试覆盖窗口中心跟随鼠标、负坐标显示器、靠近工作区边缘时收边，以及工作区小于窗口时约束尺寸；物理快捷键重复唤起与跨 DPI 显示器跟随仍需人工确认。

标题区视觉补充证据（2026-08-28）：真实 Launcher 窗口创建与完整 UI 测试会创建 13 DIP 常规标题格式，并在系统字体集合中探测 `Segoe Fluent Icons`，不存在时回退到 `Segoe MDL2 Assets`；置顶使用两套字体共同支持的 `E718`/`E77A` 图钉字形，关闭使用 `E711`。置顶、关闭命中区域及无障碍名称继续由布局与 UIA 测试契约覆盖。两套字体字形的像素级视觉效果仍需分别在 Windows 11 与 Windows 10 上人工确认。

原生弹框外观补充证据（2026-08-28）：真实设置窗口测试验证包含快捷键、屏幕边缘、开机启动和诊断日志开关，不存在主题入口或主题控件；设置、快速注册、条目属性和页面输入代码统一强制应用默认浅色 Common Controls 样式，不继承 Launcher 的材质或透明度。GroupBox、禁用文字和输入框的最终像素效果仍需 Windows 10/11 人工确认。

分页补充证据（2026-08-26）：30 条目窗口级测试与 Debug 便携运行探针验证滚轮逐行移动后启动第 6 项，普通 Grid 和完整搜索结果经 `PageDown` 后均启动第 26 项。

## 单元测试

- Hot Zone：八方向、RECT 排他边界、负坐标、DIP 转像素、角落优先。
- Dwell：进入、离开、切换热区、抑制、冷却、时钟跳变和关闭竞态。
- Search：匹配优先级、稳定排序、Unicode、空查询和 5000 条目。
- Config：默认值、范围、未知/缺失字段、损坏文件、schema 版本和高版本保护；跨版本迁移在首次定义 schema v2 时增加语料。
- Path：便携根、相对路径、跨盘、环境变量和重解析点。
- Grid：排序、跨 Tab 移动、删除 Tab 和稳定 ID。

## 集成与运行矩阵

- Windows 11 是主要运行矩阵；x64、x86、ARM64 都必须通过对应 Release 构建和二进制校验，真机运行证据按架构单独标注。Windows 10 在可取得的 22H2 ESU 或仍受支持的 LTSC 环境做尽力兼容验证，不与主要平台混报。
- 100%、125%、150%、175%、200% DPI；运行时跨屏移动。
- 单屏、左右双屏、上下双屏、L 形、负坐标和动态插拔。
- 全屏窗口、无边框游戏、视频、远程桌面和任务栏不同位置。
- Explorer 重启、会话锁定/恢复、休眠恢复和系统外观变化。
- Solid、Mica、Acrylic、Tabbed 和整体透明度边界值；关闭系统透明效果、高对比度及不支持系统背景的 Windows 降级路径。
- 快捷键冲突、第二实例、不同完整性级别与开机启动路径含空格。
- 配置只读、磁盘满、目标不存在和 UAC 取消。

## 性能测量

- 空闲 CPU、内存、热启动显示 P95 和 5000 条目搜索延迟只在优化构建中测量。
- 边缘采样基准至少持续数分钟，并同时记录采样间隔、显示器数量和全屏检测策略。
- 图标首载与暖缓存分开报告。
- 目标值见 `product-scope.md`；超标应先定位瓶颈，不通过降低正确性掩盖。

2026-08-28 首次 Release 基线：Windows 11 Pro `10.0.26200`、Intel Core i7-11700K（16 逻辑处理器）、63.8 GiB 内存，MSVC 19.51.36256、x64 `/MT`。全新独立目录配置、编译及 15/15 CTest 通过；便携目录仅含一个 1,021,952 字节（0.975 MiB）的 `HLaunch.exe`，`dumpbin /dependents` 未发现 VCRUNTIME、MSVCP 或 UCRT 动态 CRT 导入。隐藏空闲 3 秒采样为工作集 48.92 MiB、私有内存 69.78 MiB、CPU 0%；30 次第二实例 `--show` 到主窗口 `IsWindowVisible` 为 P50 46.153 ms、P95 63.184 ms、最大 64.072 ms。50 次“5000 条目”Release 测试整进程上界为 P50 32.272 ms、P95 38.254 ms、最大 45.043 ms；该值包含进程启动、构造文档、建索引与查询，不能冒充纯查询延迟。当前空闲内存和热显示 P95 未达到目标，纯搜索 20 ms 目标仍需进程内基准；本段是基线证据，不把未达标项写成已验证。

## 日志与崩溃

当前轻量日志基础设施支持 TRACE、DEBUG、INFO、WARN、ERROR；默认写入 EXE 旁 `data\logs`，只有便携数据根读写验证失败时才写入 `%LOCALAPPDATA%\HLaunch\logs`。应用已接入启动、数据加载、窗口创建、快捷键、边缘服务、托盘、激活、条目启动结果和退出事件，并安装顶层异常过滤器记录异常码、指令地址与线程号。默认不记录用户文件内容、条目名称、目标、参数、搜索词或其他敏感内容。

便携优先数据根补充证据（2026-08-28）：storage 测试 10/10、57 个断言覆盖默认创建 EXE 旁 `data`、便携路径不可用回退 AppData、已有配置文件被独占时回退，以及两处都不可用时失败。全局热键自动测试使用真实 `RegisterHotKey` 的互斥结果验证注册、冲突、保留旧绑定和重试，不再依赖会受桌面会话与完整性级别影响的 `SendInput` 模拟按键；物理按键仍属于人工运行矩阵。

默认保留最多 5 个日志文件，每个 2 MiB，并删除超过 7 天的文件；三个条件任一达到即可清理。`config.json` 的 `diagnostics.loggingEnabled` 默认开启，设置窗口可在运行时启停；关闭时关闭当前文件并卸载未处理异常过滤器，重新启用时在当前数据根创建新日志。配置保存在线程中串行合并，失败时恢复上次持久化的日志状态。

配置后台保存与日志开关补充证据（2026-08-28）：配置编解码测试 16/16、90 个断言，覆盖日志开关缺失默认和关闭值往返；存储测试 12/12、71 个断言，覆盖配置快照合并、退出刷新、修订号和失败回调；Launcher/UI 测试 46/46、388 个断言，覆盖设置窗口日志控件、应用回调和窗口复用；日志测试 4/4、15 个断言通过。Debug 便携实例 PID 41712 从 EXE 旁 `data\logs` 成功写入启动、数据加载与窗口服务事件。

页面批量启动与使用统计补充证据（2026-08-28）：Tab 菜单测试覆盖空页面禁用、非空页面启用“启动页面中的所有项目”；成功启动统计测试覆盖稳定 ID 命中、`launchCount` 递增、UTC `lastLaunchedAt` 校验和文档变更回调。Launcher/UI 测试 47/47、398 个断言，搜索测试 4/4、21 个断言，条目操作测试 15/15、96 个断言通过；Debug PID 40076 启动并保持运行。

当前不实现崩溃转储；诊断范围保持为本地滚动日志，不引入 `dbghelp`、转储配置或上传流程。

完整人工验证清单在功能范围收口后统一生成；开发过程中先维护自动测试和未覆盖的运行矩阵，避免清单随功能反复改写。

## 安全

- 不通过 Shell 拼接执行不可信命令；启动目标与参数分离。
- JSON 输入设大小与数量限制，所有整数做范围检查。
- 单实例注册消息只接受固定的 `show`、`hide`、`toggle` 标识，不接收指针、字符串或第三方自定义载荷，也不作为外部 API 发布。
- HLaunch 不实现联网检查、下载更新、遥测或其他远端通信；URL 启动由 Windows Shell 和目标应用负责。
- 日志、缓存和备份遵循最小数据原则。

## 发布门槛

V1 发布前至少需要：三架构干净 Release 构建、可执行架构的自动测试通过、支持矩阵的关键运行场景通过、可访问性验证、依赖许可证清单、无额外运行时 DLL、配置 schema 升级/回退演练以及性能结果记录。交叉编译架构尚未取得的运行证据必须单独标注，不能用 CI 通过替代。证据按 `requirements.md` 的 ID 归档，标签发布自动化见 `releasing.md`。

2026-08-27 修复验证：MSVC 19.51 x64 Debug 构建通过；15 项 CTest 中全局热键用例首轮受桌面消息时序影响失败，单独复跑通过。x64 Release 构建通过，嵌入 0.1.0 `VERSIONINFO` 与 `resources/branding/hlaunch-logo.ico` 多尺寸图标，依赖仍为系统 DLL。Launcher UIA Fragment Provider 已通过进程内真实 UI Automation 客户端验证 Name、Invoke 和 SelectionItem 调用链。

2026-08-27 交互补充：Launcher 悬浮命中自动测试覆盖真实条目、Tab、标题按钮、搜索状态 Tab 禁用以及空槽不高亮。开发阶段默认交付 Debug 构建并在修改后重新启动持续运行的 Debug 实例。

2026-08-27 Tab 排序补充：窗口消息集成测试覆盖 Tab 超过系统阈值后从首位一次拖到末位、再从末位拖到中间位置，以及未达到阈值的普通单击不提交排序。
