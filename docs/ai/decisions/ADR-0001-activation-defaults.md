# ADR-0001：激活默认值

- 状态：已接受
- 日期：2026-08-20

## 决定

全局快捷键默认开启，组合为 `Alt+Space`，行为为 Toggle。屏幕边缘唤起默认关闭；用户启用后，边缘请求只执行 Show。

## 原因与影响

快捷键提供可发现且低成本的主入口，边缘常驻采样则应由用户主动选择。`Alt+Space` 可能被 Windows 系统菜单或其他程序占用，因此注册失败必须保留有效旧配置、提示用户重新绑定，不能静默替换默认值。
